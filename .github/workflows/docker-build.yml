name: Build and Push Docker Image

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  release:
    types: [ published ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=semver,pattern={{major}}
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Create deployment artifact
      run: |
        mkdir -p artifacts
        cat > artifacts/deployment-info.json << EOF
        {
          "image": "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}",
          "tags": $(echo '${{ steps.meta.outputs.tags }}' | jq -R 'split("\n") | map(select(length > 0))'),
          "build_date": "$(date -u +%Y-%m-%dT%H:%M:%S.%3NZ)",
          "commit_sha": "${{ github.sha }}",
          "ref": "${{ github.ref }}"
        }
        EOF
        
        cat > artifacts/docker-run.sh << 'EOF'
        #!/bin/bash
        # Script to run the pages-proxy container
        # Set your environment variables in a .env file or export them before running
        
        IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
        
        docker run --rm -p 3000:3000 \
          -e PORT=3000 \
          -e ENCRYPTION_KEY="$ENCRYPTION_KEY" \
          -e GITHUB_APP_ID="$GITHUB_APP_ID" \
          -e GITHUB_INSTALLATION_ID="$GITHUB_INSTALLATION_ID" \
          -e GITHUB_WEBHOOK_SECRET="$GITHUB_WEBHOOK_SECRET" \
          -e GITHUB_APP_PRIVATE_KEY="$GITHUB_APP_PRIVATE_KEY" \
          -e CLOUDFLARE_ZONE_ID="$CLOUDFLARE_ZONE_ID" \
          -e CLOUDFLARE_API_TOKEN="$CLOUDFLARE_API_TOKEN" \
          -e CLOUDFLARE_EMAIL="$CLOUDFLARE_EMAIL" \
          "$IMAGE"
        EOF
        
        chmod +x artifacts/docker-run.sh

    - name: Upload deployment artifacts
      uses: actions/upload-artifact@v4
      with:
        name: deployment-artifacts-${{ github.run_number }}
        path: artifacts/
        retention-days: 30

    - name: Output image information
      run: |
        echo "### 🐳 Docker Image Built Successfully" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Image:** \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}\`" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Available Tags:**" >> $GITHUB_STEP_SUMMARY
        echo '${{ steps.meta.outputs.tags }}' | while read tag; do
          if [ -n "$tag" ]; then
            echo "- \`$tag\`" >> $GITHUB_STEP_SUMMARY
          fi
        done
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Pull command:**" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
        echo "docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Quick start:**" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
        echo "# Generate encryption key" >> $GITHUB_STEP_SUMMARY
        echo "export ENCRYPTION_KEY=\$(openssl rand -base64 48)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "# Set GitHub App credentials" >> $GITHUB_STEP_SUMMARY
        echo "export GITHUB_APP_ID=your_app_id" >> $GITHUB_STEP_SUMMARY
        echo "export GITHUB_INSTALLATION_ID=your_installation_id" >> $GITHUB_STEP_SUMMARY
        echo "export GITHUB_WEBHOOK_SECRET=your_webhook_secret" >> $GITHUB_STEP_SUMMARY
        echo "export GITHUB_APP_PRIVATE_KEY='your_private_key'" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "# Set Cloudflare credentials (optional if using per-installation setup)" >> $GITHUB_STEP_SUMMARY
        echo "export CLOUDFLARE_ZONE_ID=your_zone_id" >> $GITHUB_STEP_SUMMARY
        echo "export CLOUDFLARE_API_TOKEN=your_api_token" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "# Run container" >> $GITHUB_STEP_SUMMARY
        echo "docker run -d -p 3000:3000 \\" >> $GITHUB_STEP_SUMMARY
        echo "  -e ENCRYPTION_KEY -e GITHUB_APP_ID -e GITHUB_INSTALLATION_ID \\" >> $GITHUB_STEP_SUMMARY
        echo "  -e GITHUB_WEBHOOK_SECRET -e GITHUB_APP_PRIVATE_KEY \\" >> $GITHUB_STEP_SUMMARY
        echo "  -e CLOUDFLARE_ZONE_ID -e CLOUDFLARE_API_TOKEN \\" >> $GITHUB_STEP_SUMMARY
        echo "  ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Deployment artifacts are available for download with container run scripts and metadata.**" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "📖 **Documentation:** [README.md](https://github.com/${{ github.repository }})" >> $GITHUB_STEP_SUMMARY